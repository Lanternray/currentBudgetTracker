<!-- Keeps track of current budget

Key Functions:
1) should contain field of "current" budget
2) create input/button to set <starting> budget
3) create input/button to add to list (UL), two separate variables: "description" and "amount"
4) "amount" should add/subtract from "current"
5) "delete" button next to each line item, which should remove the list item and add back to "current"

-->